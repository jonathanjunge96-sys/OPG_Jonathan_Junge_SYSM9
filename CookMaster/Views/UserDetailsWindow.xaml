<Window x:Class="CookMaster.Views.UserDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="UserDetailsWindow" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Användarinformation" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Användarnamn:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding Username}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Fullständigt namn:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding FullName}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="E-post:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding Email}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Medlem sedan:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding DateJoined, StringFormat='{}{0:yyyy-MM-dd}'}"/>
            </StackPanel>
        </StackPanel>
    </Grid>

</Window>
