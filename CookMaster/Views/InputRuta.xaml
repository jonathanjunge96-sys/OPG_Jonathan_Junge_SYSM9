<UserControl x:Class="CookMaster.Views.InputRuta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookMaster.Views"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="250">
    <Grid Background="White">
        <TextBox x:Name="inputBtn" Text="" VerticalAlignment="Center" TextChanged="inputBtn_TextChanged"
                 FontSize="16" FontWeight="Light" Background="Transparent"/>
        <TextBlock x:Name="tbInput" Text="{Binding Placehold}" FontSize="16" FontWeight="Light"
                   Foreground="DarkGray" VerticalAlignment="Center" Margin="5,0,0,0" Panel.ZIndex="-1"/>
        <Button x:Name="clearBtn" Width="30" HorizontalAlignment="Right" Content="X" 
                Background="Transparent" Foreground="LightGray" BorderThickness="0" Click="clearBtn_Click"/>
    </Grid>
</UserControl>


