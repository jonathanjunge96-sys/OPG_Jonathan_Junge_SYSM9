<Window x:Class="CookMaster.Views.ForgotPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="ForgotPassword" Height="250" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtInput" VerticalAlignment="Center" 
                 Height="40" Width="250" FontSize="16" Text="{Binding BoundText, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="1" x:Name="btnForgot" FontSize="16" 
                Content="Skicka återställningslänk" Width="200" Height="40" Click="btnForgot_Click"/>
        <TextBlock Grid.Row="2" x:Name="tbResult" FontSize="16" 
                   Text="{Binding BoundText}" VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>




